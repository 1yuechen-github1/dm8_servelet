<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.Mapper.ExamMapper">

    <insert id="addExam" parameterType="org.example.entity.Exam">
        insert into "dm8"."exam_paper_library" values (#{name},#{courseId},#{createTeacher},#{startTime})
    </insert>


</mapper>